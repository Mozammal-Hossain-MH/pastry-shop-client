"use client";
import Button from "@/Shared/Button";
import React from "react";

const ButtonWrapper = ({ text }) => {
  return <Button text={text} handler={() => {}} />;
};

export default ButtonWrapper;
